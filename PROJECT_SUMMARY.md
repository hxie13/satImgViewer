# 卫星遥感影像处理与可视化项目总结

## 1. 项目概述

本项目旨在实现对批量风云/葵花系列卫星遥感影像的各类数据格式的智能解析及波段信息组合2/3维可视化，并支持按指定投影格式的png图像输出。

- **主要功能**：
  - 智能解析多种卫星数据格式
  - 支持2D地图和3D地球可视化
  - 波段组合与RGB合成
  - 多种投影系统支持
  - 图像导出功能

## 2. 项目结构

```
satImgViewer/
├── core/                # 核心功能模块
│   ├── __init__.py
│   ├── geo_utils.py     # 地理坐标工具
│   ├── image_proc.py    # 图像处理
│   ├── interfaces.py    # 接口定义
│   ├── projections.py   # 投影系统
│   └── satpy_driver.py  # 卫星数据驱动
├── ui/                  # 用户界面
│   ├── __init__.py
│   ├── canvas.py        # 2D画布
│   ├── globe_canvas.py  # 3D地球画布
│   ├── main_window.py   # 主窗口
│   ├── style.py         # 样式
│   └── widgets.py       # 自定义控件
├── utils/               # 工具模块
│   ├── __init__.py
│   └── workers.py       # 后台工作线程
├── main.py              # 程序入口
└── requirements.txt     # 依赖管理
```

## 3. 核心功能模块

### 3.1 卫星数据驱动 (satpy_driver.py)

- **功能**：负责加载和处理卫星数据文件
- **关键函数**：
  - `load_scene()`: 加载卫星数据场景
  - `_detect_reader_for_file()`: 基于文件名智能检测数据类型
  - `group_files_by_type()`: 按文件类型分组处理
  - `request_image()`: 生成图像数据
  - `export_image()`: 导出图像到文件

### 3.2 图像处理 (image_proc.py)

- **功能**：处理图像数据的归一化、校正等
- **关键函数**：
  - `normalize()`: 数据归一化
  - `apply_gamma()`: Gamma校正
  - `combine_rgb()`: RGB波段合成

### 3.3 投影系统 (projections.py)

- **功能**：提供多种地图投影支持
- **关键函数**：
  - `create_target_area()`: 创建目标投影区域
  - `extract_geographic_extent()`: 提取地理范围

### 3.4 2D可视化 (canvas.py)

- **功能**：在2D地图上显示卫星数据
- **关键函数**：
  - `update_image()`: 更新图像显示
  - `plot_pixel_mode()`: 像素模式显示

### 3.5 3D可视化 (globe_canvas.py)

- **功能**：在3D地球模型上显示卫星数据
- **关键函数**：
  - `update_texture()`: 更新地球表面纹理

### 3.6 主窗口 (main_window.py)

- **功能**：用户界面和交互管理
- **关键函数**：
  - `load_data()`: 加载数据文件夹
  - `run_process()`: 处理数据并生成图像
  - `export_image()`: 导出图像

## 4. 当前实施进度

### 已完成功能：

1. **数据格式支持**：
   - 支持NetCDF (.nc)、HDF5 (.h5, .hdf)、压缩文件 (.bz2)、原始数据 (.dat) 等格式
   - 支持风云4A/B (FY-4A/B)、葵花8/9 (Himawari-8/9) 卫星数据

2. **投影系统**：
   - 原生静止卫星投影
   - Plate Carrée 全球投影
   - Web Mercator 投影

3. **可视化功能**：
   - 2D地图视图 (基于Cartopy/Matplotlib)
   - 3D地球视图 (基于VisPy)
   - RGB波段合成
   - Gamma校正和归一化

4. **导出功能**：
   - PNG格式导出
   - GeoTIFF格式导出

5. **智能文件处理**：
   - 基于文件名的智能类型检测
   - 文件分组处理系统

### 正在改进的功能：

1. **批量处理能力**：
   - 增强对多文件同时处理的支持
   - 实现队列式处理系统

2. **高级投影系统**：
   - 添加更多投影类型支持
   - 自动投影选择

3. **性能优化**：
   - 内存使用优化
   - 处理速度提升

## 5. 数据输入输出格式

### 输入格式：

- **文件格式**：
  - NetCDF (.nc)
  - HDF5 (.h5, .hdf)
  - 压缩文件 (.bz2)
  - 原始数据 (.dat)

- **卫星类型**：
  - 风云4A/B (FY-4A/B)
  - 葵花8/9 (Himawari-8/9)

- **数据层级**：
  - L1级数据 (原始观测数据)
  - L2级数据 (产品数据)

### 输出格式：

- **图像格式**：
  - PNG (.png)
  - GeoTIFF (.tif)

- **投影选项**：
  - 原生静止卫星投影
  - Plate Carrée 全球投影
  - Web Mercator 投影

- **数据结构**：
  - RGB合成图像
  - 单波段灰度图像

## 6. 当前问题与挑战

### 技术问题：

1. **依赖管理**：
   - PyQt6模块缺失，需要在conda环境中安装
   - 部分依赖包版本兼容性问题

2. **投影系统**：
   - AreaDefinition构造函数参数变更导致的错误
   - 卫星圆盘显示问题，需要正确的地理范围设置

3. **性能问题**：
   - 处理大型卫星数据时内存使用过高
   - 多文件处理速度较慢

4. **文件类型识别**：
   - 部分非标准文件命名格式的识别准确性
   - 混合文件类型的处理逻辑

### 功能需求：

1. **批量处理**：
   - 缺乏对大规模时间序列数据的批量处理能力
   - 需要更灵活的批处理参数设置

2. **高级分析**：
   - 缺少多光谱分析工具
   - 缺乏时间序列比较功能

3. **用户界面**：
   - 界面响应速度需要优化
   - 缺少更直观的波段选择和组合工具

## 7. 下一步计划

1. **解决当前问题**：
   - 修复投影系统错误
   - 优化文件类型识别逻辑
   - 改进内存使用和处理速度

2. **功能增强**：
   - 实现批量处理工作流
   - 添加高级分析工具
   - 优化用户界面交互

3. **扩展支持**：
   - 增加对更多卫星类型的支持
   - 添加更多数据格式解析能力
   - 增强与其他GIS系统的兼容性

4. **测试与验证**：
   - 构建全面的测试数据集
   - 验证不同场景下的系统稳定性
   - 性能基准测试

## 8. 技术栈

- **核心库**：
  - Python 3.11+
  - SatPy (卫星数据处理)
  - PyQt6 (用户界面)
  - Cartopy/Matplotlib (2D可视化)
  - VisPy (3D可视化)
  - NumPy (数值计算)

- **依赖管理**：
  - Conda 环境: satImgLib

## 9. 项目价值

本项目为卫星遥感数据的处理和可视化提供了一个集成的解决方案，特别适用于：

- **气象分析**：快速处理和可视化气象卫星数据
- **环境监测**：追踪环境变化和自然灾害
- **科研应用**：为卫星数据相关研究提供工具支持
- **教育教学**：直观展示卫星数据的处理和可视化过程

通过持续的改进和扩展，本项目有望成为卫星遥感数据处理领域的重要工具。